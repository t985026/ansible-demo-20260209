# Ansible 專案總覽

## 📁 專案目錄結構

```
project/
├── docs/                           # 📚 專案文件中心
│   ├── INSTALLATION_GUIDE.md       # 🚀 快速安裝指南 (NEW)
│   ├── 01_PRD.md                   # 產品需求規格書
│   ├── 02_Technical_Architecture.md # 技術架構設計
│   ├── 03_Operation_Manual.md      # 營運操作手冊
│   └── 04_Roadmap.md               # 發展藍圖
│
├── inventory/                      # 🎯 主機清單定義
│   ├── production                  # 正式環境主機列表
│   └── staging                     # 測試環境主機列表
│
├── group_vars/                     # ⚙️ 變數配置檔
│   ├── all.yml                     # 全域變數
│   ├── webservers.yml              # Web Server 群組變數
│   └── security_targets.yml        # 安全加固群組變數
│
├── templates/                      # 📝 Jinja2 樣板檔案
│   ├── index.html.j2               # 網站首頁樣板
│   └── nginx.conf.j2               # Nginx 設定檔樣板
│
├── secrets/                        # 🔐 敏感資料 (需加密)
│   └── credentials.yml             # 密碼與金鑰
│
├── tools/                          # 🛠️ 工具類 Playbook
│   ├── connectivity_check.yml      # 網路連線測試
│   └── system_info.yml             # 系統資訊收集 (NEW)
│
├── Playbooks (主要執行檔)          # 🎬 核心自動化腳本
│   ├── preflight_check.yml         # 前置系統檢查 (NEW)
│   ├── base_system_setup.yml       # 基礎環境建置 (NEW)
│   ├── system_hardening.yml        # 系統安全加固
│   ├── web_server_setup.yml        # Web Server 部署
│   └── complete_bootstrap.yml      # 完整系統部署 (NEW)
│
├── deploy.sh                       # 🚀 互動式部署腳本 (NEW)
└── Readme.md                       # 📖 專案說明文件
```

---

## 🎯 Playbook 功能矩陣

| Playbook | 用途 | 適用情境 | 執行時間 | 重要性 |
|----------|------|----------|----------|--------|
| **preflight_check.yml** | 前置系統檢查 | 部署前驗證主機狀態 | ~1 分鐘 | 🟢 建議 |
| **base_system_setup.yml** | 基礎環境建置 | 新系統初始化 | ~5-10 分鐘 | 🔴 必要 |
| **system_hardening.yml** | 系統安全加固 | 強化系統安全性 | ~3-5 分鐘 | 🔴 必要 |
| **web_server_setup.yml** | Web Server 部署 | 需要提供 HTTP 服務時 | ~2-3 分鐘 | 🟡 可選 |
| **complete_bootstrap.yml** | 完整系統部署 | 一次性完成所有設定 | ~10-15 分鐘 | 🟢 推薦 |
| **connectivity_check.yml** | 連線測試 | 驗證網路連通性 | ~30 秒 | 🟢 建議 |
| **system_info.yml** | 系統資訊收集 | 產生系統狀態報告 | ~1 分鐘 | 🟢 建議 |

---

## 📊 部署流程圖

### 標準部署流程 (推薦)

```
┌─────────────────────────────────────────────────────────────┐
│                    開始部署                                  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 1: 前置檢查 (preflight_check.yml)                      │
│  ✓ SSH 連線測試                                              │
│  ✓ Sudo 權限驗證                                             │
│  ✓ 磁碟空間檢查 (>10GB)                                      │
│  ✓ 記憶體檢查 (>2GB)                                         │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 2: 基礎環境建置 (base_system_setup.yml)                │
│  ✓ 系統更新與套件安裝                                         │
│  ✓ 時區設定 (Asia/Taipei)                                    │
│  ✓ Python 環境配置                                           │
│  ✓ 防火牆基礎設定                                            │
│  ✓ NTP 時間同步                                              │
│  ✓ 系統效能優化                                              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 3: 系統安全加固 (system_hardening.yml)                 │
│  ✓ 建立管理帳號與稽核帳號                                     │
│  ✓ 鎖定 root 密碼登入                                        │
│  ✓ SSH 安全強化                                              │
│  ✓ Fail2Ban 防暴力破解                                       │
│  ✓ 防火牆嚴格策略                                            │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
          ┌──────────┴──────────┐
          │   需要 Web Server?   │
          └──────────┬──────────┘
                     │
          ┌──────────┴──────────┐
          │                     │
         YES                   NO
          │                     │
          ▼                     ▼
┌─────────────────────┐  ┌──────────────┐
│ web_server_setup.yml│  │  部署完成     │
│ ✓ Nginx 安裝        │  └──────────────┘
│ ✓ Web 帳號建立      │
│ ✓ 站台部署          │
│ ✓ HTTP/HTTPS 開放   │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 4: 驗證部署 (system_info.yml)                          │
│  ✓ 收集系統資訊                                              │
│  ✓ 檢查服務狀態                                              │
│  ✓ 產生部署報告                                              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
               ✅ 部署完成
```

### 快速部署 (使用 complete_bootstrap.yml)

```
┌────────────────────────┐
│ complete_bootstrap.yml │
│  (一鍵完成所有步驟)     │
└───────────┬────────────┘
            │
            ├──▶ preflight_check
            ├──▶ base_system_setup
            ├──▶ system_hardening
            └──▶ web_server_setup (可選)
            
            ▼
       ✅ 部署完成
```

---

## 🎯 使用場景指南

### 場景 1: 全新 Ubuntu 伺服器初始化

**需求**: 剛安裝完 Ubuntu 22.04，需要建立標準化環境

**執行步驟**:

```bash
# 方法 A: 使用互動式腳本
./deploy.sh
# 選擇選項 1 (前置檢查) → 選項 2 (基礎建置) → 選項 3 (安全加固)

# 方法 B: 逐步執行
ansible-playbook -i inventory/staging preflight_check.yml
ansible-playbook -i inventory/staging base_system_setup.yml
ansible-playbook -i inventory/staging system_hardening.yml
```

**預期結果**:

- ✅ 系統套件已更新
- ✅ 基礎工具已安裝
- ✅ 防火牆已啟用
- ✅ SSH 已加固
- ✅ 管理帳號已建立

---

### 場景 2: 快速部署 Web Server

**需求**: 已有基礎環境，只需部署 Nginx

**執行步驟**:

```bash
ansible-playbook -i inventory/staging web_server_setup.yml
```

**預期結果**:

- ✅ Nginx 已安裝並運行
- ✅ 自訂首頁已部署
- ✅ HTTP/HTTPS port 已開放
- ✅ 可透過 `http://<主機IP>` 存取

---

### 場景 3: 一鍵完整部署 (推薦新手)

**需求**: 全新系統，希望一次完成所有設定

**執行步驟**:

```bash
ansible-playbook -i inventory/staging complete_bootstrap.yml
```

**互動提示**:

- 是否部署 Web Server? (yes/no)
- 是否執行系統加固? (yes/no)

**預期結果**:

- ✅ 完整的基礎環境
- ✅ 強化的安全設定
- ✅ (可選) Web Server 運行中

---

### 場景 4: 批次部署多台伺服器

**需求**: 同時設定 10 台新伺服器

**步驟 1**: 編輯 `inventory/production`

```ini
[webservers]
web01 ansible_host=192.168.1.101 ansible_user=ubuntu
web02 ansible_host=192.168.1.102 ansible_user=ubuntu
web03 ansible_host=192.168.1.103 ansible_user=ubuntu
# ... 更多主機

[security_targets]
web01
web02
web03
```

**步驟 2**: 執行批次部署

```bash
ansible-playbook -i inventory/production complete_bootstrap.yml
```

**預期結果**:

- ✅ 10 台主機同時完成部署
- ✅ 設定完全一致
- ✅ 節省 90% 手動操作時間

---

## 🔧 變數配置速查表

### group_vars/all.yml (全域設定)

```yaml
admin_user: sysadmin          # 管理員帳號名稱
audit_user: auditor           # 稽核員帳號名稱
log_dir: /var/log/ansible_managed_app  # 日誌目錄
```

### group_vars/webservers.yml (Web Server 專用)

```yaml
web_root: /var/www/my_custom_site  # 網站根目錄
web_user: webadmin                 # Web 管理帳號
```

### group_vars/security_targets.yml (安全設定)

```yaml
allow_ssh_password_auth: no   # 禁用 SSH 密碼登入
```

---

## ✅ 部署檢查清單

部署完成後，請確認以下項目：

### 基礎環境

- [ ] 系統套件已更新到最新版本
- [ ] 時區設定為 Asia/Taipei
- [ ] Python 3.x 已安裝且可用
- [ ] 基礎工具 (vim, git, curl) 已安裝
- [ ] NTP 時間同步運作中

### 網路與防火牆

- [ ] 防火牆已啟用
- [ ] SSH port (22) 已開放
- [ ] (Web Server) HTTP/HTTPS port 已開放
- [ ] 預設政策為 deny incoming

### 安全性

- [ ] root 密碼登入已禁用
- [ ] SSH 密碼認證已禁用 (僅允許 Key)
- [ ] SSH 閒置逾時已設定 (300 秒)
- [ ] Fail2Ban 已安裝並運行
- [ ] 管理帳號 (sysadmin) 已建立
- [ ] 稽核帳號 (auditor) 已建立

### Web Server (如有部署)

- [ ] Nginx 服務運行中
- [ ] Web 管理帳號 (webadmin) 已建立
- [ ] 網站首頁可正常存取
- [ ] 日誌檔案正確寫入

### 驗證指令

```bash
# 測試連線
ansible -i inventory/staging all -m ping

# 收集系統資訊
ansible-playbook -i inventory/staging tools/system_info.yml

# 檢查服務狀態
ansible -i inventory/staging all -m shell -a "systemctl status sshd nginx chronyd fail2ban"
```

---

## 🚨 注意事項

### 執行前必讀

1. **備份重要資料**: 在正式環境執行前，務必備份重要設定
2. **測試環境驗證**: 先在 staging 環境測試，確認無誤後再部署到 production
3. **SSH Key 準備**: 確保 Control Node 可透過 SSH Key 連線到目標主機
4. **防火牆影響**: 執行 `system_hardening.yml` 會啟用防火牆，確保 SSH port 已開放
5. **密碼加密**: 建議使用 `ansible-vault` 加密 `secrets/credentials.yml`

### 常見錯誤處理

| 錯誤訊息 | 原因 | 解決方法 |
|---------|------|----------|
| `Permission denied` | SSH 金鑰問題 | 確認 `~/.ssh/id_rsa` 權限為 600 |
| `Sudo password required` | 需要 sudo 密碼 | 在 inventory 中設定 `ansible_become_pass` 或使用 `--ask-become-pass` |
| `Package not found` | 套件來源未更新 | 先執行 `apt update` 或 `yum update` |
| `Connection timeout` | 防火牆阻擋 | 檢查目標主機防火牆設定 |

---

## 📚 相關文件

- [快速安裝指南](docs/INSTALLATION_GUIDE.md) - 詳細的部署步驟說明
- [技術架構設計](docs/02_Technical_Architecture.md) - 了解專案架構原理
- [操作手冊](docs/03_Operation_Manual.md) - 日常維運指南

---

**維護團隊**: DevOps Engineering  
**最後更新**: 2026-01-27  
**專案版本**: v2.0
